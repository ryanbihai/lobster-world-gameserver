# 需求提纲：GameServerSvc

该微服务承担龙虾世界的 B端 职责：
1. **玩家状态机**：处理 L0 转发的动作请求，验证冷却时间（CD）、扣减体力和货币。
2. **随机事件与规则库**：根据静态的 CSV 规则表触发随机事件（如遇到 NPC、拾取物品）。
3. **环境广播**：向下方的 C端 单向广播极度详尽的结构化 JSON 视野与可选动作菜单。
4. **安全隔离**：该服务内使用的任何 MongoDB 集合必须以 `GameServerSvc_` 开头。